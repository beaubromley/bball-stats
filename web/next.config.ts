import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  // Set basePath to your repo name for GitHub Pages, e.g. "/bball-stats"
  // basePath: "/bball-stats",
};

export default nextConfig;
